class Solution {
    public static int largest(int[] arr) {
        // code here
        
        Arrays.sort(arr);
        return arr[arr.length-1];
        
       // int max = -100;
        
       // for(int i = 0; i<arr.length;i++)
       // {
    //        if(arr[i]>max)
       //     {
      //          max = arr[i];
      //      }
       // }
      //  return max;
    }
}
